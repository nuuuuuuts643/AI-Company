# T2026-0502-WORKFLOW-DEP-CLEANUP: PR #312 (test/T2026-0502-WORKFLOW-DEP-PHYSICAL-VERIFY) auto-merge 事故修復用 placeholder。
# 本ファイルは検出力テスト用で、auto-merge.yml が CI failure を待たずに merge した事故により main 流入。
# main の lint workflow を success に戻すため noop に置換。完全除去は T2026-0502-WORKFLOW-DEP-CLEANUP で予定。
name: zz removed test (noop placeholder)
on: workflow_dispatch
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Placeholder for PR #312 auto-merge accident. See lessons-learned.md."
