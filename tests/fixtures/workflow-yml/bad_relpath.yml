name: bad workflow (cd + relative path anti-pattern)
on: [push]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: fetcher deploy (broken)
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          cd projects/P003-news-timeline/lambda/fetcher
          zip -q function.zip *.py
          aws lambda update-function-code --function-name p003-fetcher --zip-file fileb://function.zip
          MERGED_ENV=$(EXISTING_ENV="$EXISTING_ENV" python3 scripts/ci_lambda_merge_env.py)
          echo "done"
