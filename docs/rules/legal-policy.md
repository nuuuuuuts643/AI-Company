# 法的・規約ポリシー

> POと協議して確定した方針。次セッションのAIが同じ確認を繰り返さなくて済むようここに残す。
> 変更時は確定日付を併記して append する（過去経緯を消さない）。

---

## 基本スタンス

「出版社に喧嘩を売らない」「配慮があると分かる形」を最優先。
著作権法32条の引用要件を満たした短い引用 + 出典明示 + 元サイトへの直リンク。

---

## コンテンツ引用ポリシー（2026-04-28 確定）

### 引用の根拠

- **著作権法32条の引用**を根拠として、ニュース記事のスニペット（1〜2文）を出典明示つきで表示する
- 引用は法的に認められた行為であり、許諾を取らずに行ってよい（ただし要件を満たすこと）

### 引用要件（必須）

| 要件 | 実装 |
|---|---|
| **出典明記** | 「〇〇（NHK等）より引用」とソース名を本文中に明示 |
| **直リンク** | 元記事への直リンク必須。リンク切れ防止のため正規URLを保持 |
| **改変禁止** | 引用文は原文のまま。誤字脱字も含めて改変しない |
| **主従関係** | 自分のコンテンツが「主」、引用部分が「従」になるよう量を抑える（1〜2文程度） |
| **AIパラフレーズ禁止** | AI生成での言い換えに差し替えない（かえって印象が悪く、引用要件も満たさなくなる） |
| **AI要約は明示** | AI が要約・言い換えした内容は「AIによる要約」と明示する |

---

## クロール・取得ポリシー（2026-04-28 確定）

### 基本方針

- **robots.txt を遵守する**（Disallow 指定パスにアクセスしない）
- **User-Agent は正直に名乗る**（例: `flotopic-bot/1.0`）。偽装しない
- **スニペット取得は RSS フィード経由を優先**（直スクレイピングより規約的に安全）
- **一次情報ドメインからの直スクレイピングは最小限に留める**（必要時のみ、頻度制御つき）

### 実装上の注意

- RSS が提供されているソースは RSS を最優先
- 直アクセス時はリクエスト間隔を空ける（連続叩き禁止）
- ソース側から拒否された場合（403/429）は即座にバックオフ＋当該ソースを一時除外

---

## 一次情報バッジの設計根拠（2026-04-28 確定）

### 設計思想

- **一次情報バッジ + 直リンクは出版社への送客になる**ため、むしろ歓迎される設計である
- 自前で完結させて送客しない設計は、引用元から見ると「持ち出し」に映るので避ける
- ソロ開発者として訴訟リスクは取れないため、**グレーより白寄りの設計を徹底する**

### 判定ロジック（実装ルール）

| ルール | 内容 |
|---|---|
| **ドメインで判定** | ソース名の文字列マッチではなく、実URL の eTLD+1 を見る |
| **ホワイトリスト方式** | 信頼できる一次情報ドメインを明示列挙して照合（NHK, 主要新聞社, 公的機関等） |
| **偽装URL を弾く** | `nhk.evil.com` のようなサブドメイン偽装は eTLD+1 が `evil.com` なので一次情報判定しない |
| **HTTPS 推奨** | http:// は中間者改ざんリスクあり。HTTPS を優先する |
| **バッジ意味** | 「視認性向上 + 元サイトへの送客」目的であり、信頼の保証ではない |

### 実装ファイル参照

- 判定ロジック: `projects/P003-news-timeline/lambda/fetcher/score_utils.py` の `is_primary_source(url)` / `PRIMARY_SOURCE_DOMAINS`
- バッジ表示: `projects/P003-news-timeline/frontend/detail.js` (記事カード/関連記事) + `frontend/style.css` `.primary-source-badge`
- スコア反映: `projects/P003-news-timeline/lambda/fetcher/scoring.py` `apply_tier_and_diversity_scoring` の ×1.2 ボーナス
- テスト: `projects/P003-news-timeline/tests/test_primary_source.py` (26ケース、偽装URL含む)

---

## 定期レビュー（重要）

- **新しい外部データソース追加時**: robots.txt + 利用規約を確認してから実装
- **新機能でコンテンツ表示方法が変わる時**: 著作権観点のセルフレビュー
- **月次**: CloudWatch ログで fetch エラーパターンを確認（bot 判定されていないか）
- 一度決めた方針でも状況変化で再確認する（セキュリティ監査と同じ考え方）

## レビューチェックリスト（実装前に確認）

1. 記事コンテンツに手を加えていないか・引用なら出典明示しているか
2. 新しいデータソース/外部API追加時に規約・robots.txt を確認したか
3. AIが生成した内容を一次情報と誤認させていないか
4. 一次情報バッジ判定はソース名文字列ではなく URL ドメインで行われているか

---

## まとめ：判断に迷ったとき

1. **白か黒か迷ったら白寄り**を選ぶ。グレーは取らない。
2. **送客になる設計**か？ 出版社が見て「うちにトラフィックが流れてくる」なら歓迎される。
3. **引用要件を物理的に満たしているか**？ 出典・直リンク・原文ママ・主従関係の4点を実装でガードする。
4. **規約・robots.txt に書かれていないからOK**ではなく、**書かれていなくても誠実に振る舞う**。

---

## 改訂履歴

- 2026-04-28: 初版確定（コンテンツ引用・クロール・一次情報バッジの3方針）
- 2026-04-28: T2026-0428-AN マージで実装ファイル参照と定期レビュー項目を統合
