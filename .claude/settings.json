{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath // \"\"' | { read -r f; echo \"$f\" | grep -q \"overview.md\" && node /Users/OWNER/ai-company/notion-sync.js; } 2>/dev/null || true",
            "statusMessage": "Syncing to Notion..."
          }
        ]
      }
    ]
  }
}
