# CEO提案ログ

CEO が出資者（POさん）に提案するアクションを蓄積するファイルです。  
承認する場合は Cowork のチャットで「承認 #XXX」と伝えてください。

---

<!-- 提案はceo_run.pyによって自動追記されます -->

---

## 提案#001（2026-04-21）

タイトル: AWS MCP接続 + P005メモリDB構築
内容: CoworkにAWS MCPサーバーを接続してCEOがAWSを直接操作できるようにする。同時にDynamoDB `ai-company-memory` テーブルを作成し、CEOの判断履歴・提案結果・学習データを蓄積する。
理由: ターミナルセッションとの情報ギャップを解消し、CEOが自律的に状況把握できる基盤を作る。メモリがあることで過去の判断を引き継げるようになる。
想定効果: CEO自走精度の向上。POへの確認コスト削減。将来的なファインチューニング用データ蓄積。
想定コスト: DynamoDB月数十円。AWS MCP自体は無料。月1,000円上限の範囲内。
ステータス: 承認済み（2026-04-21 PO口頭承認）

---

## 提案#002 （2026-04-21）

タイトル: P003品質改善実装の体制確認
内容: P003 の品質改善（エラーハンドリング強化・トピック分類精度向上・レスポンスタイム最適化）の実装主体を確認したい。CEO は lambda/ scripts/ コード編集が禁止されているため、以下いずれかの判断が必要：
  1. POが直接実装する
  2. 秘書（secretary_run.py）の機能で対応できるものに限定する
  3. 実装優先度を遅延し、現在の本番運用状態を継続する

理由: 社長から「p003開発進めてくれ」という指示があるが、実装体制が明確でないため。
想定効果: P003品質向上（方針が決定すれば実装開始可能）
想定コスト: なし（実装体制の確認のみ）

ステータス: 承認待ち

---

## 提案#003 （2026-04-23）

タイトル: home PC作業のブロッカーを今週中に解消する（最優先アクション確認）
内容: 以下の作業をPOがhome PCで実行することで、5体のエージェント有効化・HTTPS化・P004デプロイが一括解消される。所要時間は合計30〜60分と推定。

【実行順序（推奨）】
1. `git push`（5分）→ 5体のエージェントが即座にGitHub Actionsで稼働開始
2. `bash projects/P004-slack-bot/deploy.sh`（5分）→ Slack Botが有効化
3. `bash scripts/setup-domain.sh`（20分）→ CloudFront + SSL設定
4. Squarespace管理画面でネームサーバーをRoute 53のNSに変更（10分）
5. Google Cloud ConsoleでGOOGLE_CLIENT_IDを取得 → GitHub Secretsに追加（10分）

理由: 現在5体の専門AIが「git push待ち」で完全停止中。マーケティング・SEO・X投稿・開発監視・収益管理が全て機能していない。HTTPS化が完了しないとAdSense申請・flotopic.com正式公開も進められない。ブロッカー1つで連鎖している。
想定効果: git push完了で即座にX自動投稿・SEO AI・マーケティングAIが稼働開始 → Phase 1（月1,000PV獲得）のアクションが全て動き出す。HTTPS完了後はAdSense申請も可能になる。
想定コスト: 作業工数のみ（金銭的コスト追加なし）
ステータス: 承認待ち

ステータス: 承認待ち


---

## 提案#004 （2026-04-23）

タイトル: flotopic-analyticsデータ蓄積の確認と読者品質モニタリング開始
内容: CEOが毎週月曜に実施すべき「読者品質モニタリング」が、flotopic-analyticsテーブルのデータ未蓄積により機能していない。以下を確認・実施したい。

【確認事項】
- DynamoDB `flotopic-analytics` テーブルが存在するか（AWSコンソールで確認）
- P003フロントエンドの閲覧数追跡コードが正常にLambda APIを叩いているか
- Cloudflare Analyticsのトークンが正しく設定されているか（P003ブリーフィングでは「設置済み」だが、データ取得APIが未接続の可能性）

理由: 読者品質モニタリング（バイラルトピック検出・固定読者トラップ検出）はCEO憲法の必須チェック項目。データなしでは判断不能。月1,000PVのPhase 1移行判定もできない。
想定効果: 週次でPV・新規率・リピーター率が把握できるようになり、フェーズ移行判断が自動化される。バイラルトピックへの急上昇ブースト適用も可能になる。
想定コスト: ¥0（既存インフラの活用）
ステータス: 承認待ち

ステータス: 承認待ち


---

## 提案#005 （2026-04-23）

タイトル: 提案#002（P003品質改善体制）の回答を6日越えのためエスカレーション
内容: 提案#002「P003品質改善実装の体制確認」が2026-04-21から承認待ちのまま6日間経過している。CEOは1週間以上ブロッカーが未解消の場合にエスカレーション提案を出す義務がある。

【選択肢を再提示】
1. POが直接実装する（Coworkセッションで設計 → POがコードレビュー・push）
2. 現在の本番運用状態を継続し、品質改善を「Phase 1（月1,000PV）達成後」に先送りする
3. 実装範囲をドキュメント・設計書のみに限定し、コーディングはPO作業タイムに集中実施する

理由: 「p003開発進めてくれ」というPOの指示（2026-04-21）に対し、実装体制が未確定のため前進できていない。いずれかの選択肢でクローズしたい。
想定効果: 判断がクリアになり、P003改善タスクを適切なキューに入れられる。
想定コスト: なし
ステータス: 承認待ち（エスカレーション）

ステータス: 承認待ち


---

## 提案#006 （2026-04-23）

タイトル: HTTPS完了後の即実行タスクリスト事前準備（Google Search Console登録）
内容: HTTPS化（flotopic.com）完了直後に以下を即座に実行できるよう、事前準備を進めたい。

【HTTPS完了後24時間以内に実行すべきタスク（CEO主導）】
1. **Google Search Console登録** → https://search.google.com/search-console/ でflatopic.comを追加・所有権確認
2. **sitemap.xml送信** → P003のsitemap.xmlをSearch Consoleに送信してインデックス促進
3. **Google AdSense申請** → https://www.google.com/adsense/ で申請（審査2〜4週間）
4. **Bing Webmaster Tools登録** → https://www.bing.com/webmasters/ でサイト登録

【現在できる事前準備（承認後すぐ実施）】
- Googleアカウントでflatopic.comのSearch Consoleプロパティを事前登録（DNS所有権確認はHTTPS完了後でも追加可能）
- AdSenseアカウントの事前作成（申請自体はHTTPS完了後）

理由: HTTPS完了後にSearch Consoleへのインデックス申請が遅れると、SEO効果が1〜2週間後ろ倒しになる。準備しておけばHTTPS完了の当日に全て実行できる。Phase 1（月1,000PV）達成速度に直結する。
想定効果: インデックス促進で検索流入が早期に始まる。AdSense審査期間を最短化できる。
想定コスト: ¥0（全て無料サービス）
ステータス: 承認待ち

ステータス: 承認待ち
